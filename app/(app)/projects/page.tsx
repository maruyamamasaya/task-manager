import { PageHeader } from "@/components/ui/page-header";import {ProjectList} from "@/components/projects/project-list";import {createClient} from "@/lib/supabase/server";
export default async function Page(){const db=await createClient();const[{data,error},{data:tasks}]=await Promise.all([db.from("projects").select("*").order("archived").order("name"),db.from("tasks").select("project_id,progress,status")]);if(error)throw error;return <><PageHeader title="Projects" description="関連するタスクをプロジェクトにまとめます。"/><ProjectList projects={data??[]} tasks={tasks??[]}/></>}
