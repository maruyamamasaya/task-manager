"use client";
import { useState,useTransition } from "react"; import { archiveProject,createProject,renameProject } from "@/app/(app)/projects/actions"; import type { Project } from "@/types/database";
export function ProjectList({projects}:{projects:Project[]}){const [name,setName]=useState("");const [pending,start]=useTransition();const run=(p:Promise<{error?:string}>)=>start(async()=>{const r=await p;if(r.error)alert(r.error);else location.reload();});return <div className="space-y-4"><form onSubmit={e=>{e.preventDefault();if(name.trim())run(createProject(name));}} className="flex gap-2 rounded-2xl border bg-white p-4"><input value={name} onChange={e=>setName(e.target.value)} className="min-w-0 flex-1 rounded-lg border border-slate-200 px-3 py-2" placeholder="新しいプロジェクト名"/><button disabled={pending} className="rounded-lg bg-indigo-600 px-4 text-sm font-semibold text-white">作成</button></form><section className="overflow-hidden rounded-2xl border bg-white">{projects.length?projects.map(p=><article key={p.id} className={`flex items-center gap-3 border-b p-4 last:border-0 ${p.archived?"opacity-50":""}`}><span className="grid size-9 place-items-center rounded-lg bg-indigo-50 text-indigo-600">◇</span><div className="min-w-0 flex-1"><p className="truncate font-medium">{p.name}</p><p className="text-xs text-slate-400">{p.archived?"アーカイブ済み":"利用中"}</p></div><button onClick={()=>{const v=prompt("新しいプロジェクト名",p.name);if(v?.trim())run(renameProject(p.id,v));}} className="rounded-lg border px-3 py-1.5 text-sm">名前変更</button><button onClick={()=>run(archiveProject(p.id,!p.archived))} className="rounded-lg border px-3 py-1.5 text-sm">{p.archived?"復元":"アーカイブ"}</button></article>):<div className="p-12 text-center"><p className="font-semibold">プロジェクトはまだありません</p><p className="mt-2 text-sm text-slate-500">上のフォームから最初のProjectを作成しましょう。</p></div>}</section></div>}
