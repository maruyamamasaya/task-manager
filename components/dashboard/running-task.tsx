"use client";
import Link from "next/link";import {useEffect,useState,useTransition} from "react";import {stopTimer} from "@/app/(app)/phase3-actions";
export function RunningTask({logId,taskId,title,startedAt}:{logId:string;taskId:string;title:string;startedAt:string}){const [now,setNow]=useState(Date.now()),[pending,start]=useTransition();useEffect(()=>{const id=setInterval(()=>setNow(Date.now()),1000);return()=>clearInterval(id)},[]);const seconds=Math.max(0,Math.floor((now-new Date(startedAt).getTime())/1000));const clock=[Math.floor(seconds/3600),Math.floor(seconds%3600/60),seconds%60].map(n=>String(n).padStart(2,"0")).join(":");return <section className="mb-6 rounded-2xl border-2 border-indigo-300 bg-indigo-50 p-5"><p className="text-sm font-bold text-indigo-700">● 作業中</p><div className="mt-2 flex flex-wrap items-center justify-between gap-4"><div><h2 className="text-xl font-bold">{title}</h2><p className="mt-1 font-mono text-2xl tabular-nums">{clock}</p></div><div className="flex gap-2"><Link href={`/tasks?task=${taskId}`} className="rounded-lg border bg-white px-4 py-2 text-sm">Taskを開く</Link><button disabled={pending} onClick={()=>start(()=>{void stopTimer(logId)})} className="rounded-lg bg-indigo-600 px-4 py-2 text-sm text-white disabled:opacity-50">作業終了</button></div></div></section>}
