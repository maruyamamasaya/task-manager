"use client";
import type { WbsItem } from "@/lib/wbs/types"; import { saveWbsItem } from "@/app/(app)/wbs/actions";
export function WbsItemEditor({projectId,item,parentId,onClose}:{projectId:string;item:WbsItem|null;parentId:string|null;onClose:()=>void}){return <div className="fixed inset-0 z-50 grid place-items-center bg-slate-950/40 p-4" onMouseDown={e=>{if(e.currentTarget===e.target)onClose()}}><form action={async f=>{const r=await saveWbsItem(projectId,item?.id??null,parentId,f);if(!r?.error)onClose();else alert(r.error)}} className="max-h-[90vh] w-full max-w-2xl overflow-y-auto rounded-xl bg-white p-6 shadow-xl"><div className="flex justify-between"><h2 className="text-lg font-bold">{item?"項目を編集":"項目を追加"}</h2><button type="button" onClick={onClose}>✕</button></div><div className="mt-5 grid gap-4 sm:grid-cols-2"><Field label="名前" name="name" value={item?.name} required/><Field label="担当者名" name="owner_name" value={item?.owner_name}/><Field label="開始日" name="start_date" type="date" value={item?.start_date}/><Field label="終了日" name="end_date" type="date" value={item?.end_date}/><label className="text-sm">ステータス<select name="status" defaultValue={item?.status??"not_started"} className="mt-1 w-full rounded-lg border p-2"><option value="not_started">未着手</option><option value="in_progress">進行中</option><option value="completed">完了</option><option value="on_hold">保留</option></select></label><Field label="進捗率" name="progress" type="number" value={item?.progress??0} min="0" max="100"/><Field label="予定工数" name="estimate_hours" type="number" value={item?.estimate_hours} step="0.01" min="0"/><Field label="実績工数" name="actual_hours" type="number" value={item?.actual_hours} step="0.01" min="0"/></div><Area label="説明" name="description" value={item?.description}/><Area label="備考" name="note" value={item?.note}/><div className="mt-5 flex justify-end gap-2"><button type="button" onClick={onClose} className="rounded-lg border px-4 py-2">キャンセル</button><button className="rounded-lg bg-indigo-600 px-4 py-2 font-semibold text-white">保存</button></div></form></div>}
function Field({label,value,...p}:{label:string;name:string;value?:string|number|null;type?:string;required?:boolean;min?:string;max?:string;step?:string}){return <label className="text-sm">{label}<input {...p} defaultValue={value??""} className="mt-1 w-full rounded-lg border p-2"/></label>}function Area({label,name,value}:{label:string;name:string;value?:string|null}){return <label className="mt-4 block text-sm">{label}<textarea name={name} defaultValue={value??""} rows={3} className="mt-1 w-full rounded-lg border p-2"/></label>}
